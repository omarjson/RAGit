export function Settings() {
  return (
    <div className="p-6">
      <h1 className="mb-1 text-2xl font-semibold">Settings</h1>
      <p className="mb-6 text-sm text-zinc-400">
        Mode (Local / Team), model, GPU layers, context size, embedding model, scheduler, and user management.
      </p>
      <div className="rounded-lg border border-dashed border-zinc-800 p-10 text-center text-zinc-500">
        Configuration panel here. (Phase6–7)
      </div>
    </div>
  );
}
